llama-cpp-python
langchain
langchain-core
langchain-community>=0.0.10
redis

# Pinecone Vector Database
# The package was renamed from `pinecone-client` to `pinecone`.
# Use the new package name. Pin to a recent stable version to avoid unexpected breaks.
pinecone>=2.2.0
sentence-transformers

# Firebase Firestore Database
firebase-admin

# FastAPI & Web Server
fastapi
uvicorn[standard]
pydantic

# Additional dependencies for embeddings
torch
transformers
